
@{
    ViewBag.Title = "Following Me List";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Following Me List</h2>

<div class="row">
    <ul class="course">
        @foreach (var follow in Model.Followings)
        {
            <li>
                <div class="details">
                    <span class="lecturer">
                        @follow.Name
                    </span>
                </div>
            </li>
        }
    </ul>
</div>